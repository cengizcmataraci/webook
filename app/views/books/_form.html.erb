<div class="card" align="center" class="p-3 mb-2">
  <div class="card-body">
<%= form_with(model: book, local: true) do |form| %>
  <% if book.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(book.errors.count, "error") %> prohibited this book from being saved:</h2>

      <ul>
        <% book.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <p> If you want your book to be available, choose Yes on the status and if you want your book to be tradeable, again, choose Yes on the tradeablestatus. </p>
<% end %>


<%= simple_form_for @book do |f| %>
  <%= f.input :title %>
  <%= f.input :author %>
  <%= f.input :pagecount %>
  <%= f.input :status,   as: :radio_buttons %>
  <%= f.input :tradeablestatus,   as: :radio_buttons %>
  <%= f.button :submit, class: "btn-primary" %>
<% end %>

</div>
</div>